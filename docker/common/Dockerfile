FROM golang:1.24-alpine AS builder

WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o /out/dns-error-agent ./cmd/dns-error-agent

FROM alpine:3.20

RUN apk add --no-cache ca-certificates

COPY --from=builder /out/dns-error-agent /usr/local/bin/dns-error-agent

ENTRYPOINT ["/usr/local/bin/dns-error-agent"]
